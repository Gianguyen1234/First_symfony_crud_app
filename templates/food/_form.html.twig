{{ form_start(form) }}
    <div class="mb-3">
        {{ form_widget(form) }}
    </div>
    <button type="submit" class="btn btn-primary">
        <i class="bx bx-save"></i> {{ button_label|default('Save') }}
    </button>
{{ form_end(form) }}
